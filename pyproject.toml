[project]
name = "azathoth"
authors = [
	{ name = "Fernando Bryan Reza Campos", email = "fer.rezac@outlook.com" },
]
version = "0.1.0"
description = "Some of the stuff I've created while learning the 'State of the Art' in AI."
requires-python = ">=3.12"
readme = "README.md"
license = { file = "LICENSE" }
keywords = [
	"ai", 
	"deep learning"
	]
dependencies = [
	# "numpy", # Numerical computing
	# # "pandas>=1.4.0",        # Data manipulation
	# "polars>=1.34.0", # Fast DataFrame library in Rust
	# * MCP
	"mcp[cli]>=1.16.0",
	# Some:
	"httpx>=0.28.1",
	"gitingest>=0.3.1",
	"rich>=14.1.0",
]

# Not necessary, I will need to add them if I sometime publish this as a package
# exclude = [
# # rust related stuff
# 	"uv.lock",
# 	"Cargo.toml",
# 	"Cargo.lock",
# 	".github",
# 	"azathoth-core/*",
# 	"azathoth-macros/*",
# ]


[tool.pyright]
venvPath = "."
venv = ".venv"

[project]
name = "mcp-py"
authors = [
	{ name = "Fernando Bryan Reza Campos", email = "fer.rezac@outlook.com" },
]
version = "0.0.1"
description = "Some of the stuff I've created while learning the 'State of the Art' in AI."
requires-python = ">=3.12"
readme = "README.md"
license = { file = "LICENSE" }
keywords = [
	"ai",
	"mcp",
	"deep learning"
]
dependencies = [
	# Logging
	"rich>=14.2.0",
	"rune-lib>=0.0.4",
	# * MCP
	"mcp[cli]>=1.17.0",
]

# # Not necessary, I will need to add them if I sometime publish this as a package
# ignore = [
# # rust related stuff
# 	"uv.lock",
# 	"Cargo.toml",
# 	"Cargo.lock",
# 	".github",
# 	"azathoth-core/*",
# 	"azathoth-macros/*",
# ]

# [tool.uv.workspace]
# members = [
#     "mcp-py",
# ]

[tool.pyright]
venvPath = "."
venv = ".venv"

[project.scripts]
main-tester = "mcp_py:main"
# cli = "mcp_py.cli:main"

[build-system]
requires = ["uv_build>=0.8.19,<0.9.0"]
build-backend = "uv_build"
